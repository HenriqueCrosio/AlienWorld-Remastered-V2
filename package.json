{
  "name": "alienworld-remastered",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "probe": "node scripts/probe.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "sharp": "^0.35.3",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
